# TON

1. Создать `TON_API_KEY` через [бота](https://t.me/toncenter)
2. Создать файл `.env` и добавить ключ туда (пример: `.env.example`)

Установка зависимостей:
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Запуск:
```bash
python main.py
```
